apiVersion: v1
kind: Service
metadata:
  name: helm-practice-service
spec:
  selector:
    app: helm-practice
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP
